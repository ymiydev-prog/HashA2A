[pytest]
markers =
    e2e: End-to-end tests against live APIs (requires network)
    smoke: Tests that work without any credentials (pure logic)
    hedera: Tests that need HEDERA_OPERATOR_ID / HEDERA_OPERATOR_KEY
    twitter: Tests that need Twitter API credentials
