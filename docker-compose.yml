version: "3.9"

services:
  hasha2a:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src
    volumes:
      - ./static:/app/static
    restart: unless-stopped
